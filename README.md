# Simple frequency tracker

## Quick-start

```
cmake .
make
python3 bgs.py
```

### More info

If `fwdpy11` is not fully-installed, then prepend `cmake` and the execution of the script with `PYTHONPATH=/the/path/to/fwdpy11`.
